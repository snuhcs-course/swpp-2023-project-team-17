<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.GoClass" parent="Theme.Material3.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="android:statusBarColor">@color/black</item>
        <item name="android:windowBackground">@color/bg_gray</item>
        <item name="android:textAppearance">@style/SemiBold</item>
    </style>

    <style name="Theme.GoClass" parent="Base.Theme.GoClass" />

    <style name="Theme.GoClass.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.GoClass.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>