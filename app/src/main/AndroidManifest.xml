<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"
        tools:ignore="CoarseFineLocation" />
    <uses-permission android:name="com.android.alarm.permission.SET_ALARM" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:name=".GoClassApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GoClass"
        tools:targetApi="31">
        <activity
            android:name=".ProfessorAttendanceListActivity"
            android:exported="false" />
        <activity
            android:name=".StudentAttendanceActivity"
            android:exported="false"
            android:label="@string/title_activity_student_attendance"
            android:theme="@style/Theme.GoClass.NoActionBar" />
        <activity
            android:name=".ProfessorAttendanceActivity"
            android:exported="false"
            android:label="@string/title_activity_professor_attendance"
            android:theme="@style/Theme.GoClass.NoActionBar" />
        <activity
            android:name=".ClassActivity"
            android:exported="false"
            android:label="@string/title_activity_class" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <service android:name=".service.LocationService"></service>
        <service
            android:name=".service.AttendanceService"
            android:enabled="true"
            android:exported="false" ></service>
        <receiver
            android:name=".AttendanceReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>
    </application>

</manifest>